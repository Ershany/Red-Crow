'use strict'

process.env.NODE_ENV = 'test';

let chai = require('chai')
let assert = chai.assert
let should = chai.should()

let search = require('../util/error_codes')

describe('Error Codes', () => {
	
})

// for(let i = 0; i < 62; i++)
// 	console.log(convertErrorCode(i))